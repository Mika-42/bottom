#ifndef TELNET_CONNEXION_H
#define TELNET_CONNEXION_H
#include <sys/types.h>
#include <stdlib.h>
#include <libtelnet.h>
#include <unistd.h> 
typedef struct {
    int sockfd;
    telnet_t *telnet_ptr;
} telnet_session;

void telnet_end_session(telnet_session session);

telnet_session telnet_connexion_init(char *host, int port, char *user, char *password);


int telnet_dry_run(telnet_session session);

#endif
